public class CalculatorRunner 
{
    public static void main(String[] args) 
    {
        // 1. Slope between (1, 1) and (5, 10)
        double slopeResult = Calculator.slope(1, 1, 5, 10);
        System.out.println("Slope: " + slopeResult);

        // 2. Distance between (2, 3) and (6, 7)
        double distanceResult = Calculator.distance(2, 3, 6, 7);
        System.out.println("Distance: " + distanceResult);

        // 3. Roots of x^2 + 2x - 8 = 0
        String roots = Calculator.quadRoots(1, 2, -8);
        System.out.println("Roots: " + roots);
    }
}
